apiVersion: v1
kind: Service
metadata:
  name: i27-helpdesk-notification
  namespace: ${NAMESPACE}
spec:
  selector:
    app: i27-helpdesk-notification
  ports:
    - name: http
      port: 80
      targetPort: 8084
  type: ClusterIP
